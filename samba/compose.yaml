services:
  samba:
    image: dockurr/samba
    container_name: samba
    environment:
      NAME: "Data"
      USER: ${SAMBA_USER}
      PASS: ${SAMBA_PASS}
      TZ: ${TZ}
    ports:
      - 445:445
    volumes:
      - ${SAMBA_DIR}:/storage
    restart: always
